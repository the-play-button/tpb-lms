<!-- template-version: 1 -->
# CHANGELOG — tpb-lms

> Dispatch public dérivé. Régénéré déterministe par `tpb-app-agents regen-changelog`. **Ne JAMAIS éditer entre les balises auto-gen** (les balises XML-comments délimitent un block re-écrasé à chaque régen).
>
> Sources : `git log` (commits du cycle) + `graphify --diff` (deltas symboles + edges + god nodes) + extraits de `JOURNAL/weekly-wNN.md` (narrative compactée).
>
> Toute édition manuelle entre les balises sera écrasée au prochain régen.

<!-- changelog:auto:retired -->
<!-- graphify pipeline retired 2026-05-31 — Plan 23.
     The auto-generated section is preserved in git history.
     The changelog is now manually curated. -->


---

## Archive

Cycles plus anciens que 500 cycles → automatiquement archivés dans `JOURNAL/CHANGELOG.archive.md`. Le présent fichier ne garde que les 500 plus récents.
