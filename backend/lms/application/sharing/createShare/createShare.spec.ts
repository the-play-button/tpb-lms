import { describe, it, expect } from 'vitest';

describe('createShare', () => {
  it('skeleton spec — replace with real assertions on createShareHandle output', () => {
    expect(typeof 'createShare').toBe('string');
  });
});
